# 功能清单

## 图片加载

| 功能 | 说明 | 状态 |
|------|------|------|
| 打开文件夹 | 原生文件夹选择器，递归扫描所有图片 | ✅ |
| 打开图片 | 文件选择器，选中图片自动创建虚拟分组 | ✅ |
| 拖入文件夹 | 识别为文件夹，添加到目录树 | ✅ |
| 拖入图片 | 识别为文件，自动创建虚拟分组 | ✅ |
| 混合拖入 | 同时拖入文件和文件夹，分别处理 | ✅ |
| 图片去重 | 按路径去重，重复加载不重复显示 | ✅ |
| 支持格式 | jpg/jpeg、png、webp、bmp、gif、tif/tiff、jxl、avif、svg、ico、pcx | ✅ |
| 渐进式扫描 | 后台线程遍历，每扫描完一个目录立即渲染，无需等待全量完成 | ✅ |
| 扫描状态指示 | 顶部加载栏显示旋转 spinner + "扫描中"，不阻挡操作 | ✅ |

## 视图模式

### 紧凑模式（分组关闭）
- 所有图片在同一网格中平铺显示
- 虚拟分组作为独立区域插入

### 目录分组模式（分组开启）
- 树状结构展示文件夹层级
- 根节点 = 加载的文件夹（显示父路径 + 文件夹名）
- 子节点 = 子文件夹（居中显示文件夹名）
- 叶子节点 = 图片网格
- 拖入图片自动创建虚拟分组（📦 节点）

## 网格视图

| 功能 | 说明 | 状态 |
|------|------|------|
| 按需加载 | 图片进入视图才加载 base64（支持并发控制） | ✅ |
| 加载状态 | 加载中 spinner + 文件名 + 文件大小；加载失败/跳过显示文件名+大小+「已跳过」标签 | ✅ |
| 文件信息 | hover 显示文件名（加载中/加载失败同样显示） | ✅ |
| 大小阈值跳过 | 可在通用设置中配置（MB），超过大小的图片跳过加载仅显示占位符 | ✅ |
| 查看时加载跳过图片 | 大小跳过开启时可选，开启后点击大图/对比时仍尝试加载原图 | ✅ |
| 宽高比自适应 | 高度 = 格子大小，宽度按图片比例自适应 | ✅ |
| 滚轮缩放 | Ctrl+滚轮在主体区域全局调整格子大小 (10~400px) | ✅ |
| 格子样式 | 圆角、间距、背景色（设置面板） | ✅ |
| 右键菜单 | 右键弹出：查看 / 复制图片 / 复制路径 / 重命名 / 另存为 / 元信息 / 资源管理器打开 / 默认程序打开 | ✅ |
| 元信息弹窗 | 右键→元信息 或大图操作栏元信息按钮，显示所有 EXIF 字段 + UserComment + 文件创建/修改时间，每行可单独复制，底部「复制全部」 | ✅ |
| 复制图片到剪贴板 | PNG 直接写入，其他格式通过 createImageBitmap + canvas 转为 PNG 后写入 | ✅ |
| 另存为 | 原生保存对话框，保存后自动增量刷新 | ✅ |
| 全局浏览器右键菜单 | 全局禁用浏览器默认右键菜单，统一使用自定义菜单 | ✅ |

## 大图查看

| 功能 | 说明 | 状态 |
|------|------|------|
| 蒙灰背景 | 大图打开时背景半透明遮罩 | ✅ |
| 滚轮缩放 | 直接滚轮放大/缩小 | ✅ |
| 平移模式 | 点击平移按钮后拖动图片 | ✅ |
| 适应窗口 | 一键恢复缩放 | ✅ |
| 全屏 | 全屏查看 | ✅ |
| 左右切换 | 按钮 / 键盘方向键 | ✅ |
| 跨节点切换 | Ctrl+←/→ 在文件夹节点间切换（后序遍历：子→父→兄弟） | ✅ |
| 分组内导航 | 分组模式下左右切换限制在当前小分组内 | ✅ |
| 操作栏 | 底部悬浮，hover 显示 | ✅ |
| 文件信息 | 文件名、缩放比、索引、大小、分辨率；信息显隐按钮带 active 状态指示 | ✅ |
| 打开菜单 | 资源管理器定位 / 默认程序打开 / 自定义外部程序 | ✅ |
| 右键菜单 | 右键弹出：关闭 / 复制图片 / 复制路径 / 重命名 / 另存为 / 元信息 / 资源管理器打开 / 默认程序打开 | ✅ |
| 元信息弹窗 | 显示所有 EXIF 字段 + UserComment + 文件创建/修改时间，每行可单独复制，底部「复制全部」 | ✅ |
| 复制图片到剪贴板 | 通过 Web Clipboard API 写入 PNG（其他格式自动转为 PNG） | ✅ |
| 另存为 | 原生保存对话框，支持选择路径和文件名，保存后自动增量刷新 | ✅ |
| 关闭 | Esc 键 / 关闭按钮 / 点击背景关闭 | ✅ |

## 图片标记

| 功能 | 说明 | 状态 |
|------|------|------|
| 5级标记系统 | 图片可标记 1-5 级，标记颜色在设置中可自定义 | ✅ |
| 网格视图标记边框 | 有标记的图片显示对应颜色的 3px outline 边框 | ✅ |
| 网格视图标记角标 | 右下角显示彩色圆形徽标+等级数字（可缩放跟随） | ✅ |
| 网格视图右键菜单 | 空白处右键弹出切换模式菜单（仅显示非当前模式选项） | ✅ |
| 标记开关 | 主控制栏标记按钮可一键切换所有标记边框/角标的显示 | ✅ |
| 角标独立开关 | 设置中可独立控制是否显示标记角标 | ✅ |
| 标记菜单（副控制栏） | 选择模式下显示，提供标记/全选/取消选择三种操作 | ✅ |
| 大图标记 | 大图查看器操作栏有标记按钮和菜单 | ✅ |
| 大图标记信息 | 图片信息栏显示标记彩色圆标+等级数字 | ✅ |
| 标记快捷键 | 1-5 键快速标记，` 键和 0 键取消标记（大小键盘均支持） | ✅ |

## 选择模式

| 功能 | 说明 | 状态 |
|------|------|------|
| 查看模式 | 点击图片打开大图 | ✅ |
| 选择模式 | 点击切换选中（默认多选） | ✅ |
| 模式切换 | 底部控制栏切换 | ✅ |
| 切换清空 | 切回查看/选择模式时自动清空图片和节点选中 | ✅ |
| Shift 连选 | Shift+点击选择范围内的所有图片；再次 Shift+点击已选中图片则取消范围；跨节点自动重置 | ✅ |
| 全选/反选/一级图片 | 节点标题右侧的操作按钮：全选节点下所有图片、反选、仅选一级图片（节点既有子节点又有图片时显示） | ✅ |
| 勾选节点 | 节点标题左侧勾选框，可选中整个文件夹节点 | ✅ |
| 节点内选中计数 | 选择模式下，文件夹计数从 `(12)` 变为 `(3/12)` 显示选中数/总数；部分选中时选中数字标蓝，全部选中时整体标蓝 | ✅ |
| 节点内选中状态提示 | 文件夹标题展开箭头颜色反映节点内图片选中状态（灰=无，蓝=部分/全部） | 🚧 |
| 操作菜单 | 选中后「操作」按钮弹出菜单：移动、复制、删除 | ✅ |
| 移动 | 弹出文件夹选择，优先 `fs::rename` 同卷瞬间完成，跨卷回退复制+删除；保留创建时间等元数据；保留目录结构；同名自动添加 `-副本` 后缀；同路径自动跳过；移动后自动清理空目录；操作后自动增量刷新 | ✅ |
| 复制 | 弹出文件夹选择，保留目录结构复制文件，同名自动添加 `-副本` 后缀；操作后自动增量刷新 | ✅ |
| 移动至此 | 文件夹标题 hover 显示「移动至此」按钮，选择模式+有选中时可见。点击后直接以该节点为目标移动，走完整移动逻辑（含确认、刷新） | ✅ |
| 复制至此 | 文件夹标题 hover 显示「复制至此」按钮，逻辑同移动至此 | ✅ |
| 删除 | 确认对话框后删除到回收站；选中文件夹节点时同时删除文件夹本身 | ✅ |
| 大量图片警告 | 选中 >100 张时弹出菜单和确认对话框突出提示 | ✅ |
| 新分组 | 选中图片后创建虚拟分组 | ✅ |
| 对比 | 选中 2-8 张图片后可进入多图对比视图 | ✅ |

## 对比视图

| 功能 | 说明 | 状态 |
|------|------|------|
| 多图对比 | 支持 2-8 张图片同时对比，2 张时全屏单对比，3+ 张时三角/矩阵网格 | ✅ |
| 两两对比 | 自动生成所有图片对的两两比较视图（clip-path 分割） | ✅ |
| 拖动分割线 | 可拖拽的垂直分割线控制左图显示范围，所有单元共享同一进度 | ✅ |
| 缩放 | Ctrl+滚轮或底部按钮缩放 (0.1x~10x)，5% 步进 | ✅ |
| 全局/局部模式 | 全局模式缩放平移整个网格；局部模式缩放平移所有单元内部的图片 | ✅ |
| 适应窗口 | 一键恢复缩放和偏移 | ✅ |
| 平移模式 | 切换后拖拽平移查看 | ✅ |
| 视图模式切换 | 左三角(i>j) / 右三角(i<j) / 全矩阵(含对角线) 三种布局循环切换 | ✅ |
| 矩阵视图 | 全 N×N 网格，对角线留空，上下三角分别显示 i/j 和 j/i | ✅ |
| 悬浮操作栏 | 底部 hover 显示：适应窗口/放大/缩小/平移/百分比/关闭/文件名/坐标轴/局部/视图模式 | ✅ |
| 文件名标签 | 每个单元左上/右上显示对应图片文件名，可切换显隐 | ✅ |
| 坐标轴 | 冻结首行首列的 Excel 风格坐标轴，显示文件名，可切换显隐，可选深浅交替 | ✅ |
| 文件名标签自适应 | 文件名字号根据图片总数自动缩小（N=3→10px, N=8→6px） | ✅ |
| 右键菜单 | 右键弹出：退出对比 / 文件名显隐 / 坐标轴显隐 / 视图模式切换 / 复制路径 / 复制图片 / 另存为 | ✅ |
| 关闭 | Esc 键或关闭按钮 / 点击背景关闭 | ✅ |

## 筛选与排序

| 功能 | 说明 | 状态 |
|------|------|------|
| 三模式筛选 | 文件（文件名）/ 分组（文件夹名+虚拟分组标题）/ 路径（完整目录路径） | ✅ |
| 正则筛选 | 按正则匹配 | ✅ |
| 筛选预设 | 设置面板添加，下拉快速切换 | ✅ |
| 筛选联动 | 有匹配的分组自动展开，无匹配的自动折叠 | ✅ |
| 排序 | 文件名 / 修改日期 / 文件大小 | ✅ |
| 排序方向 | 升序 / 降序 | ✅ |

## 目录操作

| 功能 | 说明 | 状态 |
|------|------|------|
| 在资源管理器中打开 | 每个节点标题左侧悬浮文件夹图标按钮，点击在资源管理器中打开 | ✅ |
| 删除根节点 | hover 显示 ✕ 按钮，删除时保留其他根路径覆盖的图片 | ✅ |
| 删除子节点 | 每个可折叠子节点 hover 显示 ✕ 按钮，从父根路径中排除 | ✅ |
| 删除虚拟分组 | 每个虚拟分组有删除按钮 | ✅ |
| 选择模式删除文件夹 | 选中文件夹节点后通过「操作→删除」可将文件夹本身移到回收站，子路径自动去重避免重复删除 | ✅ |
| 全部折叠/展开/折叠叶子 | 分组模式下控制栏折叠菜单（合并为一个按钮，弹出下拉选项） | ✅ |
| 紧凑模式开关 | 控制栏一键切换分组紧凑模式 | ✅ |
| 标题显隐 | 分组模式下切换标题显示（含虚拟分组） | ✅ |
| 大文件夹自动折叠 | 根节点 >5 子文件夹或 >100 张图时，后续新增节点默认折叠 | ✅ |
| 彩虹层级着色 | 按深度轮换彩色背景（可自定义颜色列表、数量） | ✅ |
| 节点网格间距 V/H | 设置中可分别调整节点之间的垂直/水平间距（默认4px） | ✅ |
| 分组紧凑模式 | 非根节点自适应宽度 inline-flex 排列，可多节点共享一行 | ✅ |
| 统一内容网格 | 紧凑模式下子节点和图片在同一个 flex-wrap 容器中混合排列 | ✅ |
| 层级压缩 | 隐藏只有一个子文件夹的中间节点，路径合并到子节点标题（彩虹色分段） | ✅ |
| 标题栏紧凑 | 紧凑模式下隐藏打开/移动/复制/移除按钮，相关操作移入右键菜单 | ✅ |
| 标题栏右键菜单 | 右键标题弹出菜单：全选/反选/一级图片→移动/复制→查看/选择模式→文件名→打开/复制路径→移除 | ✅ |

## 虚拟分组

| 功能 | 说明 | 状态 |
|------|------|------|
| 创建虚拟分组 | 选中图片/文件夹节点后创建（支持带子目录结构） | ✅ |
| 添加/移除选中项 | 选择模式下虚拟根节点的 +/− 按钮，支持一级节点和图片 | ✅ |
| 保存到文件夹 | 虚拟根节点悬浮保存按钮，选择目录后复制所有图片（保留子目录结构）；>100 张时确认提示 | ✅ |
| 保存后替换分组 | 保存后询问是否加载该文件夹并替换当前虚拟分组（先扫描加载再移除原分组） | ✅ |
| 删除虚拟分组 | 每个虚拟分组有删除按钮 | ✅ |

## 底部控制栏

| 功能 | 说明 | 状态 |
|------|------|------|
| 双栏布局 | 主控制栏 + 副控制栏（SelectionBar）上下排列 | ✅ |
| 悬浮式布局 | `position: fixed` 固定在底部，不占布局空间 | ✅ |
| 毛玻璃背景 | `backdrop-filter: blur(12px)` 半透明背景 | ✅ |
| 自动隐藏 | 大图查看/对比模式时自动隐藏 | ✅ |
| 主控制栏 | 分组/标题/折叠菜单/紧凑/彩虹/查看-选择模式/标记开关/文件名常驻/清空 | ✅ |
| 副控制栏（选择模式） | 已选计数/新分组/对比/标记菜单（标记选择）/操作菜单 | ✅ |

## 标题样式

| 功能 | 说明 | 状态 |
|------|------|------|
| 居中布局 | 文件夹名+张数居中，折叠箭头在标题前，✕ 在右 | ✅ |
| 文字颜色 | 设置中可分别配置根节点/子节点标题文字色 | ✅ |
| 背景颜色 | 设置中可分别配置根节点/子节点标题背景色（彩虹模式下按深度轮替） | ✅ |
| 递归计数 | 每个节点显示其子树中所有图片总数 | ✅ |
| 根路径显示 | 根节点显示父路径 + 文件夹名，子节点仅显示文件夹名 | ✅ |
| Hover 高亮 | 整个标题条 hover 时半透明白色叠加层提亮 | ✅ |

## 快捷键（可自定义）

| 动作 | 默认键 | 替代键 | 范围 | 状态 |
|------|--------|--------|------|------|
| 关闭 | Esc | - | 全局/查看器/对比 | ✅ |
| 上一张 | ← | - | 查看器 | ✅ |
| 下一张 | → | - | 查看器 | ✅ |
| 放大 | + | = | 查看器/对比 | ✅ |
| 缩小 | - | - | 查看器/对比 | ✅ |
| 删除 | Delete | - | 查看器 | ✅ |
| 取消标记 | ` | 0 | 查看器 | ✅ |
| 标记 1-5 | 1-5 | - | 查看器 | ✅ |
| 缩放(滚轮) | Ctrl+滚轮 | - | 网格视图 | ✅ |
| 缩放(滚轮) | 滚轮 | - | 查看器/对比 | ✅ |

## 配置持久化

| 功能 | 说明 | 状态 |
|------|------|------|
| 自动保存 | 设置变更后 500ms 防抖保存到 exe 同目录 config-user.yaml | ✅ |
| 自动加载 | 启动时自动读取配置文件 | ✅ |

## 文件变更检测

| 功能 | 说明 | 状态 |
|------|------|------|
| 自动监听 | 使用 `notify`  crate 递归监听已加载文件夹 | ✅ |
| 变更提示 | 检测到外部文件变更时刷新按钮显示红色脉冲提示点 | ✅ |
| 细粒度刷新 | 有点击刷新仅处理变更的文件（增/删/改），无点则全量重扫 | ✅ |
| 自操作抑制 | 自身删除/移动/复制时抑制监听事件，避免误提示；操作后自动调用 `applyFileChanges` 增量刷新 | ✅ |
| 路径规范化 | Windows 路径大小写/分隔符不敏感比较 | ✅ |

## 其他

| 功能 | 说明 | 状态 |
|------|------|------|
| 重命名 | 网格右键 / 大图右键 / 大图操作栏均可触发；弹出输入框预填文件名（不含扩展名）；同名忽略；重名时提示使用 `-copy` 后缀；成功后自动增量刷新 | ✅ |
| 设置面板（分页） | 通用/网格/颜色/查看器/快捷键/高级 六个标签页 | ✅ |
| 标记颜色配置 | 5 级标记颜色独立配置，角标开关 | ✅ |
| 可自定义快捷键 | 每个动作支持主键+替代键独立编辑，一键重置 | ✅ |
| 外部打开方式 | 设置中配置程序路径（支持浏览选择），大图打开菜单中显示 | ✅ |
| 多语言 | 中文 / English，设置中切换，即时生效 | ✅ |
| 命令行参数 | 启动时传入图片/文件夹路径，自动加载 | ✅ |
| 清空 | 一键清除所有加载的图片 | ✅ |
